<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GolfingStats.HomePage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="New Round" Clicked="NavNewRound" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <Grid Padding="5" RowSpacing="1" ColumnSpacing="1" BackgroundColor="Default">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label x:Name="statusMessage"
           Grid.Row="2" />

            <Button Text="Get All Rounds"
            Grid.Row="3"
            Clicked="OnGetButtonClicked" />

            <ListView x:Name="RoundList"
              Grid.Row="4">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell Height="30">
                            <StackLayout Padding="5">
                                <Label Text="{Binding GolfCourse}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </ContentPage.Content>
</ContentPage>